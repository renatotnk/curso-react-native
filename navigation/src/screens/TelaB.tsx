import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { TextoCentral } from '../components'

type Props = {}

const TelaB = (props: Props) => {
	return <TextoCentral corFundo='#3b82c4'>Tela B</TextoCentral>
}

export default TelaB

const styles = StyleSheet.create({})
